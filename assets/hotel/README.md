# Royal Hotel polish

Cream facade, shaded sash windows, cornice blocks, iron balcony scrollwork, decorative brackets, lantern and framed ROYAL HOTEL sign. Simplified details were guided by the user-provided Royal Hotel Randwick photograph. Existing two-storey game proportions are preserved.

Base SVG viewBox: 890 137 297 353. ART_ASSETS bounds remain [-10,137,297,353], drawn at the existing (900,0) hotel anchor. The original open-target drawing is still applied over the facade at exactly its original coordinates. drawHotelTrim adds only frame/closed-door details, outside the open success zones. Hitboxes debug is unchanged.

The HTML embeds the SVG for standalone offline use. Update both the SVG and data URL for future artwork changes. Preview shows the facade only; gameplay applies the existing level-specific openings above it.

Hellmen effective approach reach is now 57.6 world units (48 x 1.2). Only assistZone changed: left padding 41.6 plus the existing 16-unit torso radius, with far and vertical edges unchanged. The existing catch detector, cost, animation, dialogue and rescue priority are untouched.

Validation: 2,549 gameplay checks, 48 updated range checks and 8 hotel checks passed. Source comparison permits only the hotel asset, hotel trim drawing and assistZone change. Archibald and jet rendering are byte-for-byte unchanged. Static artwork preview inspected; live Chrome rendering remains unverified.
